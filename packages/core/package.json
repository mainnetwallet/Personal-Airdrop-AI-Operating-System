{
  "name": "@airdrop-os/core",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@airdrop-os/types": "workspace:*",
    "@airdrop-os/security": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
