{
  "name": "@airdrop-os/api",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.ts",
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/rate-limit": "^9.1.0",
    "ioredis": "^5.4.1",
    "uuid": "^9.0.1",
    "@airdrop-os/config": "workspace:*",
    "@airdrop-os/database": "workspace:*",
    "@airdrop-os/security": "workspace:*",
    "@airdrop-os/identity": "workspace:*",
    "@airdrop-os/types": "workspace:*",
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "tsx": "^4.16.5",
    "vitest": "^2.0.5",
    "@types/uuid": "^9.0.8",
    "dotenv-cli": "^7.4.2"
  },
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}