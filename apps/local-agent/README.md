# apps/local-agent

Status: **NOT_CONFIGURED** (Phase 1 scaffold only)

The PC agent (Node.js/TypeScript, Playwright-based browser automation,
device auth, checkpoint sync, human handoff) is built in Phase 6
(Browser / PC Agent / Extension / Workflow / Checkpoint). It will
authenticate as a `PC`-type device against the `/auth` and `/devices`
endpoints built in Phase 1.
